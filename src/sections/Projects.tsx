import { ProjectsShowcase } from './projects/ProjectsShowcase';

/** @deprecated Use ProjectsShowcase — kept for existing imports. */
export function Projects() {
  return <ProjectsShowcase />;
}

export { ProjectsShowcase };
